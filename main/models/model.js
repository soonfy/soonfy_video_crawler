"use strict";
{
    _id,
        film_id,
        site,
        uri,
        created_at,
        status;
    0 / 1,
    ;
}
{
    _id: site + id,
        created_at,
        film_plist_id,
        crawled_at,
        crawl_status;
}
{
    _id: film_plist_episode_id + date,
        film_plist_episode_id,
        date;
    Date;
    value,
        is_real;
    created_at: ;
}
{
    _id: film_plist_id + date,
        film_plist_id,
        date,
        value;
    calculated_at;
}
